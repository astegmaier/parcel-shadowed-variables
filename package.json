{
  "name": "stackoverflow-parcel-global-variables",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html --no-optimize --no-source-maps --no-scope-hoist",
    "serve": "http-server dist",
    "serve:raw": "http-server raw --port 8090",
    "clean": "rimraf dist .parcel-cache"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.0.1"
  },
  "devDependencies": {
    "http-server": "^14.0.0",
    "rimraf": "^3.0.2"
  }
}
